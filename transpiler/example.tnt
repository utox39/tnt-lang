import stdio.h

fn main() -> int {
  let foo: int = 0;
  let foo_ptr: ref int = addr foo;

  printf("foo: %d\n", foo);
  defer printf("defer foo: %d\n", foo + 1);

  if (foo == 1) {
    tnt ->foo_ptr;
  }

  if (foo != 0) {
    tnt "foo != 0";
  }

  return 0;
}
